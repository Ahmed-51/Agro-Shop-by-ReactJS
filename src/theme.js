import { createTheme } from "@mui/material";

const themes = createTheme({
    palette: {
        primary: {
            main: '#6CBE03'
        },
        secondary: {
            main: '#326eb3'
        }
    },
});

export default themes;